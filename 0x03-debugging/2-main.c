#include "main.h"

/**
 * main - check code for largest number function
 * Return: 0
 */
int main(void)
{
    int a, b, c;

    a = 972;
    b = -98;
    c = 0;

    printf("%d is the largest number\n", largest_number(a, b, c));

    return (0);
}
